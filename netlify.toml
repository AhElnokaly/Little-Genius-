[build]
  command = "npm run build"
  publish = "dist"

# +++ أضيف بناءً على طلبك: إعدادات التوجيه لتطبيق الصفحة الواحدة (SPA) على Netlify +++
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
